# ── Supabase ──────────────────────────────────────────────────────────────────
# Get these from: https://app.supabase.com → Your Project → Settings → API
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...your-service-role-key...

# ── Vite public Supabase key (frontend) ───────────────────────────────────────
VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...your-anon-key...

# ── Wasabi S3 (Drive file storage) ────────────────────────────────────────────
# Get these from: https://console.wasabisys.com → Access Keys
WASABI_ACCESS_KEY_ID=your-wasabi-access-key-id
WASABI_SECRET_ACCESS_KEY=your-wasabi-secret-access-key

# Bucket name — must match the bucket you create in Wasabi
WASABI_BUCKET=eastape-share

# Region & endpoint — see table below for all regions:
# us-east-1     → https://s3.us-east-1.wasabisys.com
# us-east-2     → https://s3.us-east-2.wasabisys.com
# us-west-1     → https://s3.us-west-1.wasabisys.com
# eu-central-1  → https://s3.eu-central-1.wasabisys.com
# eu-west-1     → https://s3.eu-west-1.wasabisys.com
# ap-northeast-1 → https://s3.ap-northeast-1.wasabisys.com
WASABI_REGION=us-east-1
WASABI_ENDPOINT=https://s3.us-east-1.wasabisys.com

# ── Bunny Stream (Media video hosting) ────────────────────────────────────────
# Get these from: https://dash.bunny.net → Stream → Your Library
# BUNNY_STREAM_API_KEY      → Stream → API Key (top of library page)
# BUNNY_STREAM_LIBRARY_ID   → Stream → Your Library → Library ID
# BUNNY_STREAM_CDN_HOSTNAME → Stream → Your Library → Pull Zone hostname
#   (e.g. vz-abc123.b-cdn.net  or  your-custom-hostname.com)
BUNNY_STREAM_API_KEY=your-bunny-stream-api-key
BUNNY_STREAM_LIBRARY_ID=123456
BUNNY_STREAM_CDN_HOSTNAME=vz-xxxxxxxx.b-cdn.net

# Same value exposed to the Vite frontend for iframe embeds & thumbnail URLs
VITE_BUNNY_STREAM_LIBRARY_ID=123456
VITE_BUNNY_STREAM_CDN_HOSTNAME=vz-xxxxxxxx.b-cdn.net

# ── Frontend ──────────────────────────────────────────────────────────────────
# Your deployed Vercel domain — used for CORS.
# During development leave it empty or use http://localhost:5173
FRONTEND_URL=https://your-app.vercel.app

# ── Vite (frontend only — MUST be prefixed VITE_) ─────────────────────────────
# Leave empty when frontend & API are on the same Vercel domain.
# Only set this if you split into two separate projects.
VITE_API_BASE=
